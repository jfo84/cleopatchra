{{#ember-table
  rows=rows
  columns=columns
  as |row|
}}
  {{#each pulls as |pull|}}
    {{#ember-table-row
      row=row
      as |cell|
    }}
      {{pull-request pull=pull}}
    {{/ember-table-row}}
  {{/each}}
{{/ember-table}}